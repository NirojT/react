export interface ICourseData {
    title: string;
    description: string;
    tagline: string;
    criteria: string
    duration: string;
}

export interface ICourseDataError {
    title: string;
    description: string;
    tagline: string;
    criteria: string
    duration: string;
}